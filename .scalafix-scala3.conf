# Scala 3 (3.3 LTS) does not record unused-import diagnostics in semanticdb,
# so RemoveUnused/removeUnused is unsupported until 3.4 — reorder only.
rules = [OrganizeImports]

OrganizeImports {
  removeUnused = false
  targetDialect = Scala3
}
